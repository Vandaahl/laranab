# These are specific to Laranab
# You can set multiple API's by separating them with a pipe character, like this: https://api-1.com|https://api-2.com
NEWZNAB_APIS=
TMDB_API_KEY=

# DB credentials for MariaDB container - change the passwords from the default values before creating the containers
MARIADB_ROOT_PASSWORD=root_password
MARIADB_USER=laranab
MARIADB_PASSWORD=password
MARIADB_DATABASE=laravel

# DB credentials for app container
DB_CONNECTION=mariadb
DB_HOST=mariadb
DB_PORT=3306
DB_DATABASE="${MARIADB_DATABASE}"
DB_USERNAME="${MARIADB_USER}"
DB_PASSWORD="${MARIADB_PASSWORD}"

# These are for the Laravel app
APP_NAME=Laranab
APP_ENV=production
APP_KEY=
APP_DEBUG=false
APP_URL=http://localhost
APP_TIMEZONE=